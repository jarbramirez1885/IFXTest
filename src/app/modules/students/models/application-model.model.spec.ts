import { ApplicationModel } from './application-model.model';

describe('ApplicationModel', () => {
  it('should create an instance', () => {
    expect(new ApplicationModel()).toBeTruthy();
  });
});
